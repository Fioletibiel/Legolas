import sys
import matplotlib.pyplot as plt
import networkx as nx


class Graf(object):

    def __init__(self):

        # G = nx.grid_2d_graph(4, 3)
        # G = nx.complete_graph(5)
        # try:
        #     nx.write_adjlist(G, sys.stdout)  # write adjacency list to screen
        # except TypeError:
        #     nx.write_adjlist(G, sys.stdout.buffer)  # write adjacency list to screen
        #
        # nx.write_edgelist(G, path="grid.edgelist", delimiter=":")
        # H = nx.read_edgelist(path="grid.edgelist", delimiter=":")
        #
        # nx.draw(H)
        # plt.show()

        # z = [2,2,2,1,1]
        # print(nx.is_graphical(z))
        # G = nx.configuration_model(z)
        # nx.draw(G)
        # plt.show()

        # G = nx.MultiDiGraph()
        # G.add_edge("a", "b", message="msg")
        # G.add_edge("a", "c", message="msg")
        # G.add_edge("b", "c", message="msg")
        # nx.draw(G)
        # plt.show()

        # G = nx.generators.directed.random_k_out_graph(5, 3, 1)
        # pos = nx.layout.spring_layout(G)
        # ax = plt.gca()
        # ax.set_axis_off()
        # plt.show()
        # nx.draw(G)
        # plt.show()

        pass